/**
 * Adds two values using JavaScript's + operator.
 * Intended for numeric addition, but follows JS semantics.
 * @param {number} x
 * @param {number} y
 * @returns {number} x + y
 */
export function addNumbers(x, y) {
  return x + y;
}
export default addNumbers;